/** Generated by english-annotation-buster, Powered by Google Translate.**/
/*
 * Copyright 2002-2016 the original author or authors.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      https://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * 版权所有2002-2016的原始作者。 
 * 根据Apache许可证2.0版（"许可证"）获得许可； 
 * 除非遵守许可，否则不得使用此文件。 
 * 您可以在https://www.apache.org/licenses/LICENSE-2.0上获得许可的副本。 
 * 除非适用法律要求或以书面形式同意，否则根据"许可"分发的软件将按"现状"分发，没有任何明示或暗示的保证或条件。 
 * 有关许可下特定的语言管理权限和限制，请参阅许可。 
 * 
 */

package org.springframework.core.env;

import org.springframework.core.convert.support.ConfigurableConversionService;
import org.springframework.lang.Nullable;

/**
 * Configuration interface to be implemented by most if not all {@link PropertyResolver}
 * types. Provides facilities for accessing and customizing the
 * {@link org.springframework.core.convert.ConversionService ConversionService}
 * used when converting property values from one type to another.
 *
 * @author Chris Beams
 * @since 3.1
 */
/**
 * 大多数（如果不是全部）{<@link> PropertyResolver}类型都将实现配置接口。 
 * 提供用于访问和自定义将属性值从一种类型转换为另一种类型时使用的{@link  org.springframework.core.convert.ConversionService ConversionService}的功能。 
 *  @author 克里斯·比姆斯@since 3.1
 */
public interface ConfigurablePropertyResolver extends PropertyResolver {

	/**
	 * Return the {@link ConfigurableConversionService} used when performing type
	 * conversions on properties.
	 * <p>The configurable nature of the returned conversion service allows for
	 * the convenient addition and removal of individual {@code Converter} instances:
	 * <pre class="code">
	 * ConfigurableConversionService cs = env.getConversionService();
	 * cs.addConverter(new FooConverter());
	 * </pre>
	 * @see PropertyResolver#getProperty(String, Class)
	 * @see org.springframework.core.convert.converter.ConverterRegistry#addConverter
	 */
	/**
	 * 返回在属性上执行类型转换时使用的{@link  ConfigurableConversionService}。 
	 *  <p>返回的转换服务的可配置性质允许方便地添加和删除各个{@code  Converter}实例：<pre class ="code"> ConfigurableConversionService cs = env.getConversionService（）; cs.addConverter（new FooConverter（））; </ pre> 
	 * @see  PropertyResolver＃getProperty（String，Class）
	 * @see  org.springframework.core.convert.converter.ConverterRegistry＃addConverter
	 */
	ConfigurableConversionService getConversionService();

	/**
	 * Set the {@link ConfigurableConversionService} to be used when performing type
	 * conversions on properties.
	 * <p><strong>Note:</strong> as an alternative to fully replacing the
	 * {@code ConversionService}, consider adding or removing individual
	 * {@code Converter} instances by drilling into {@link #getConversionService()}
	 * and calling methods such as {@code #addConverter}.
	 * @see PropertyResolver#getProperty(String, Class)
	 * @see #getConversionService()
	 * @see org.springframework.core.convert.converter.ConverterRegistry#addConverter
	 */
	/**
	 * 设置{@link  ConfigurableConversionService}在属性上执行类型转换时使用。 
	 *  <p> <strong>注意</ strong>：作为完全替换{@code  ConversionService}的替代方法，请考虑通过钻入{@link  #getConversionService来添加或删除单个{@code  Converter}实例（）}和调用方法，例如{@code  #addConverter}。 
	 *  
	 * @see  PropertyResolver＃getProperty（String，Class）
	 * @see  #getConversionService（）
	 * @see  org.springframework.core.convert.converter.ConverterRegistry＃addConverter
	 */
	void setConversionService(ConfigurableConversionService conversionService);

	/**
	 * Set the prefix that placeholders replaced by this resolver must begin with.
	 */
	/**
	 * 设置必须由此解析器替换的占位符的前缀。 
	 * 
	 */
	void setPlaceholderPrefix(String placeholderPrefix);

	/**
	 * Set the suffix that placeholders replaced by this resolver must end with.
	 */
	/**
	 * 设置此解析器替换的占位符必须以的后缀。 
	 * 
	 */
	void setPlaceholderSuffix(String placeholderSuffix);

	/**
	 * Specify the separating character between the placeholders replaced by this
	 * resolver and their associated default value, or {@code null} if no such
	 * special character should be processed as a value separator.
	 */
	/**
	 * 指定由此解析器替换的占位符及其关联的默认值之间的分隔字符，如果没有此类特殊字符不应作为值分隔符处理，则指定{@code  null}。 
	 * 
	 */
	void setValueSeparator(@Nullable String valueSeparator);

	/**
	 * Set whether to throw an exception when encountering an unresolvable placeholder
	 * nested within the value of a given property. A {@code false} value indicates strict
	 * resolution, i.e. that an exception will be thrown. A {@code true} value indicates
	 * that unresolvable nested placeholders should be passed through in their unresolved
	 * ${...} form.
	 * <p>Implementations of {@link #getProperty(String)} and its variants must inspect
	 * the value set here to determine correct behavior when property values contain
	 * unresolvable placeholders.
	 * @since 3.2
	 */
	/**
	 * 设置遇到嵌套在给定属性值内的无法解析的占位符时是否引发异常。 
	 *  {@code  false}值表示严格的分辨率，即将引发异常。 
	 *  {@code  true}值指示应以无法解析的$ {...}形式传递无法解析的嵌套占位符。 
	 *  <p> {<@link> #getProperty（String）}及其变体的实现必须检查此处设置的值，以便在属性值包含不可解析的占位符时确定正确的行为。 
	 *  @3.2起
	 */
	void setIgnoreUnresolvableNestedPlaceholders(boolean ignoreUnresolvableNestedPlaceholders);

	/**
	 * Specify which properties must be present, to be verified by
	 * {@link #validateRequiredProperties()}.
	 */
	/**
	 * 指定必须存在哪些属性，以通过{@link  #validateRequiredProperties（）}进行验证。 
	 * 
	 */
	void setRequiredProperties(String... requiredProperties);

	/**
	 * Validate that each of the properties specified by
	 * {@link #setRequiredProperties} is present and resolves to a
	 * non-{@code null} value.
	 * @throws MissingRequiredPropertiesException if any of the required
	 * properties are not resolvable.
	 */
	/**
	 * 验证是否存在由{@link  #setRequiredProperties}指定的每个属性，并将其解析为非{@code  null}的值。 
	 *  
	 * @throws  MissingRequiredPropertiesException如果任何必需的属性都不可解析。 
	 * 
	 */
	void validateRequiredProperties() throws MissingRequiredPropertiesException;

}
