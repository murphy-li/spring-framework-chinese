/** Generated by english-annotation-buster, Powered by Google Translate.**/
/*
 * Copyright 2002-2018 the original author or authors.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      https://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * 版权所有2002-2018的原始作者。 
 * 根据Apache许可证2.0版（"许可证"）获得许可； 
 * 除非遵守许可，否则不得使用此文件。 
 * 您可以在https://www.apache.org/licenses/LICENSE-2.0上获得许可的副本。 
 * 除非适用法律要求或以书面形式同意，否则根据"许可"分发的软件将按"现状"分发，没有任何明示或暗示的保证或条件。 
 * 有关许可下特定的语言管理权限和限制，请参阅许可。 
 * 
 */

package org.springframework.test.web.reactive.server;

import java.util.function.Consumer;

import reactor.core.publisher.Flux;

/**
 * {@code ExchangeResult} variant with the response body decoded as
 * {@code Flux<T>} but not yet consumed.
 *
 * @author Rossen Stoyanchev
 * @since 5.0
 * @param <T> the type of elements in the response body
 * @see EntityExchangeResult
 */
/**
 * 响应主体已解码为{@code  Flux <T>}但尚未使用的{@code  ExchangeResult}变体。 
 *  @author  Rossen Stoyanchev @从5.0起
 * @param  <T>响应主体中元素的类型
 * @see  EntityExchangeResult
 */
public class FluxExchangeResult<T> extends ExchangeResult {

	private final Flux<T> body;


	FluxExchangeResult(ExchangeResult result, Flux<T> body) {
		super(result);
		this.body = body;
	}


	/**
	 * Return the response body as a {@code Flux<T>} of decoded elements.
	 *
	 * <p>The response body stream can then be consumed further with the
	 * "reactor-test" {@code StepVerifier} and cancelled when enough elements have been
	 * consumed from the (possibly infinite) stream:
	 *
	 * <pre>
	 * FluxExchangeResult&lt;Person&gt; result = this.client.get()
	 * 	.uri("/persons")
	 * 	.accept(TEXT_EVENT_STREAM)
	 * 	.exchange()
	 * 	.expectStatus().isOk()
	 * 	.expectHeader().contentType(TEXT_EVENT_STREAM)
	 * 	.expectBody(Person.class)
	 * 	.returnResult();
	 *
	 * StepVerifier.create(result.getResponseBody())
	 * 	.expectNext(new Person("Jane"), new Person("Jason"))
	 * 	.expectNextCount(4)
	 * 	.expectNext(new Person("Jay"))
	 * 	.thenCancel()
	 * 	.verify();
	 * </pre>
	 */
	/**
	 * 将响应主体作为已解码元素的{@code  Flux <T>}返回。 
	 *  <p>然后可以使用"reactor-test"{<@code> StepVerifier}进一步消耗响应主体流，并在（可能是无限的）流中消耗了足够的元素时取消响应体流：<pre> FluxExchangeResult <Person>结果= this.client.get（）.uri（"/ persons"）.accept（TEXT_EVENT_STREAM）.exchange（）.expectStatus（）。 
	 * isOk（）.expectHeader（）。 
	 * contentType（TEXT_EVENT_STREAM）.expectBody（Person.class）。 
	 *  returnResult（）; StepVerifier.create（result.getResponseBody（））.expectNext（new Person（"Jane"），new Person（"Jason"））.expectNextCount（4）.expectNext（new Person（"Jay"））.thenCancel（）。 
	 * 校验（）; </ pre>
	 */
	public Flux<T> getResponseBody() {
		return this.body;
	}

	/**
	 * Invoke the given consumer within {@link #assertWithDiagnostics(Runnable)}
	 * passing {@code "this"} instance to it. This method allows the following,
	 * without leaving the {@code WebTestClient} chain of calls:
	 * <pre class="code">
	 *	client.get()
	 * 		.uri("/persons")
	 * 		.accept(TEXT_EVENT_STREAM)
	 * 		.exchange()
	 * 		.expectStatus().isOk()
	 *	 	.returnResult()
	 *	 	.consumeWith(result -> assertThat(...);
	 * </pre>
	 * @param consumer consumer for {@code "this"} instance
	 */
	/**
	 * 在{@link  #assertWithDiagnostics（Runnable）}中调用给定的使用者，将{@code "this"}实例传递给它。 
	 * 此方法允许以下操作，而无需离开{@code  WebTestClient}调用链：<pre class ="code"> client.get（）.uri（"/ persons"）.accept（TEXT_EVENT_STREAM）.exchange（） .expectStatus（）。 
	 * isOk（）.returnResult（）.consumeWith（result-> assertThat（...）; </ pre> 
	 * @param  {{@code>"this"}实例的消费者消费者
	 */
	public void consumeWith(Consumer<FluxExchangeResult<T>> consumer) {
		assertWithDiagnostics(() -> consumer.accept(this));
	}

}
